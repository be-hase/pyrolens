{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  // docker:pinDigests keeps the `@sha256:...` pin on every FROM (Dockerfile,
  // Dockerfile.release, dev/compose.yaml) in step with the tag, so the base
  // images stay reproducible without hand-updating digests on every bump.
  // Note: a digest-only re-push of the same tag (no version bump) may not
  // carry an age minimumReleaseAge can gate on — it delays new tags, not
  // necessarily every digest change behind an unchanged one.
  extends: ['config:recommended', 'docker:pinDigests'],
  automerge: true,
  major: {
    automerge: false,
  },
  // Supply chain mitigation: don't pick up a release until it has been public for a while,
  // so compromised versions are usually detected and unpublished before automerge sees them.
  // Renovate's vulnerabilityAlerts updates bypass this delay by default.
  minimumReleaseAge: '7 days',
  labels: ['renovate'],
  packageRules: [
    // The four runtime dependencies are the ones a bump can actually break
    // for users, and CI cannot see all of it: the unit and browser tests say
    // the app still works, not that it still looks right. Review those by
    // hand; everything else (devDependencies, Actions, base images, the load
    // generator) can merge itself once CI is green.
    {
      matchManagers: ['npm'],
      matchDepTypes: ['dependencies'],
      automerge: false,
    },
  ],
}
